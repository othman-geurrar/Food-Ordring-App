import hero from "../assets/hero1.png"
const Hero = () => {
  return (
    <div>
        <img src={hero} alt=""  className="w-full max-h-[650px] object-cover"/>
    </div>

  )
}

export default Hero;